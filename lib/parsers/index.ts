/**
 * Excel (SheetJS/xlsx) parsers that turn uploaded monthly workbooks into typed
 * records for the database. Added in later phases — see the roadmap in CLAUDE.md.
 *
 * Each parser should validate its input and return a discriminated result so
 * the upload UI can surface row-level errors.
 */

export {};
