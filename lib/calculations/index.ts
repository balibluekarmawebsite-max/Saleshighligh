/**
 * Business calculations (variances, occupancy, ADR, RevPAR, ROAS, booking pace,
 * forecasts, …). Added in later phases — see the roadmap in CLAUDE.md.
 *
 * Keep pure, side-effect-free functions here so they can be unit-tested and
 * reused across server components, API routes, and the report exporter.
 */

export {};
