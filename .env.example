# Local development uses SQLite. The Prisma schema is written to be
# Postgres-compatible, so for production switch the datasource provider to
# "postgresql" and point DATABASE_URL at your Postgres instance, e.g.:
#   DATABASE_URL="postgresql://user:password@host:5432/bk_sales?schema=public"
DATABASE_URL="file:./dev.db"
